#include<iostream>

using namespace std;

int main () {
    int i;
    cin >> i;
    
    int x;
    int counter = 1;
    cout << "At the position " << i << " there is a(n) ";
    
    while (cin >> x and counter != i) {
        ++counter;
    }
    cout << x << '.' << endl;
}
